pipeline {

    agent any

    stages {

        stage('Install Dependencies') {
            steps {
                // sh 'npm install'
                echo 'npm install successfully'
            }
        }

        stage('Build') {
            steps {
                echo 'Build successful 🚀'
            }
        }

    }
}
